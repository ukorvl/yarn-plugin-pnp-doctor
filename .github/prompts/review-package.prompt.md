# Review Package

Review the npm package and Yarn plugin bundle for publish readiness.

Run:

```bash
yarn check
yarn verify:package
```

Inspect:

- `package.json` metadata
- `files` allowlist
- generated bundle under `bundles/`
- tarball contents
- package smoke test output
