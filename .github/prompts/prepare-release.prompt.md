# Prepare Release

Prepare this Yarn plugin for release.

1. Run `yarn check`.
2. Run `yarn verify:package`.
3. Inspect the generated tarball.
4. Confirm `package.json` version.
5. Draft release notes focused on user-facing command, preset, packaging, and compatibility changes.

Publishing happens from the GitHub release workflow.
